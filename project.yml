environment:
  DB_HOST: "{DB_HOST}"
  DB_PORT: "{DB_PORT}"
  DB_DATABASE: "{DB_DATABASE}"
  DB_USER: "{DB_USER}"
  DB_PASSWORD: "{DB_PASSWORD}"
packages:
  - name: directory
    actions:
      - name: tenant
        runtime: 'nodejs:default'
        limits:
          timeout: 5000